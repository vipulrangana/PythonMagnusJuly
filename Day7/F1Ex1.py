from FunctionWithReturnEx import multi,sub,add #for import all the functions use *

y = int(input('Enter the first value : '))
z = int(input('Enter the second value : '))

print(multi(y,z))
print(sub(y,z))
print(add(y,z))